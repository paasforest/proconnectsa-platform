# PostgreSQL Database Configuration
DB_NAME=procompare_db
DB_USER=procompare_user
DB_PASSWORD=your_secure_password_here
DB_HOST=localhost
DB_PORT=5432

# Set to True to use SQLite for development
USE_SQLITE=False

# Django Settings
DEBUG=False
SECRET_KEY=your_secret_key_here
ALLOWED_HOSTS=your-domain.com,localhost,127.0.0.1

# Email Configuration (for notifications)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your_email@gmail.com
EMAIL_HOST_PASSWORD=your_app_password

# Redis Configuration (for WebSocket)
REDIS_URL=redis://localhost:6379/0

# PRODUCTION EMAIL CONFIGURATION
# Choose one of these email providers:

# Option 1: SendGrid (RECOMMENDED)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.sendgrid.net
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=apikey
EMAIL_HOST_PASSWORD=your_sendgrid_api_key
DEFAULT_FROM_EMAIL=ProConnectSA <noreply@proconnectsa.co.za>

# Option 2: Gmail Business
# EMAIL_HOST=smtp.gmail.com
# EMAIL_HOST_USER=your_business_email@proconnectsa.co.za
# EMAIL_HOST_PASSWORD=your_app_password

# Option 3: Amazon SES
# EMAIL_HOST=email-smtp.us-east-1.amazonaws.com
# EMAIL_HOST_USER=your_ses_access_key
# EMAIL_HOST_PASSWORD=your_ses_secret_key

# Option 4: Mailgun
# EMAIL_HOST=smtp.mailgun.org
# EMAIL_HOST_USER=postmaster@mg.proconnectsa.co.za
# EMAIL_HOST_PASSWORD=your_mailgun_password
